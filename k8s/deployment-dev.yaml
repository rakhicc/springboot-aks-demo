apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: springboot-dev
  template:
    metadata:
      labels:
        app: springboot-dev
    spec:
      containers:
        - name: springboot-app
          image: rakhiacr12345.azurecr.io/springboot-aks-demo:v1
          ports:
            - containerPort: 8080

          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"


            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secret-dev
                  key: DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret-dev
                  key: DB_PASSWORD


      imagePullSecrets:
        - name: acr-secret